import MainLayout from '@/layouts/main-layout';
import { HeroSection } from './partials/HeroSection';

const Home = () => {
  return (
    <MainLayout>
      <HeroSection />
    </MainLayout>
  );
};

export default Home;
